import React from 'react';
import { Route } from 'react-router';
import Application from 'components/application';

export default (
	<Route name='index' component={Application} path='/'>
	</Route>
);
